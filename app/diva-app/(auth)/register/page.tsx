import { RegisterForm } from "@/components/diva/auth/register-form";

export const metadata = { title: "Create Account — DIVA Growth Capital" };

export default function DivaRegisterPage() {
  return <RegisterForm />;
}
